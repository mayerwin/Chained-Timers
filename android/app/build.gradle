apply plugin: 'com.android.application'

android {
    namespace = "com.mayerwin.chainedtimers"
    compileSdk = rootProject.ext.compileSdkVersion
    defaultConfig {
        applicationId "com.mayerwin.chainedtimers"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 3
        versionName "1.1.1"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        aaptOptions {
             // Files and dirs to omit from the packaged assets dir, modified to accommodate modern web apps.
             // Default: https://android.googlesource.com/platform/frameworks/base/+/282e181b58cf72b6ca770dc7ca5f91f135444502/tools/aapt/AaptAssets.cpp#61
            ignoreAssetsPattern = '!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~'
        }
    }
    // ------------------------------------------------------------------
    // Sideload signing.
    //
    // Both debug and release builds are signed with a stable keystore
    // committed to the repo (../sideload.keystore). This is intentional:
    // for sideload distribution (no Play Store), the signing key just
    // needs to be the SAME between successive APK versions so Android
    // recognises them as updates to the same app rather than a different
    // developer trying to overwrite. Without this, every CI build is
    // signed with a fresh ephemeral debug key and users have to uninstall
    // before each update — which also wipes their saved chains.
    //
    // The keystore password is "sideload" and is committed in plaintext
    // because the key isn't a secret in this trust model — anyone could
    // build their own fork and sign it the same way. For a future Play
    // Store release, replace this signingConfig with one backed by a
    // secret keystore.
    // ------------------------------------------------------------------
    signingConfigs {
        sideload {
            storeFile     file('../sideload.keystore')
            storePassword 'sideload'
            keyAlias      'chainedtimers'
            keyPassword   'sideload'
            storeType     'PKCS12'
        }
    }
    buildTypes {
        debug {
            signingConfig signingConfigs.sideload
        }
        release {
            signingConfig signingConfigs.sideload
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

repositories {
    flatDir{
        dirs '../capacitor-cordova-android-plugins/src/main/libs', 'libs'
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
    implementation "androidx.core:core-splashscreen:$coreSplashScreenVersion"
    implementation project(':capacitor-android')
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
    implementation project(':capacitor-cordova-android-plugins')
}

apply from: 'capacitor.build.gradle'

try {
    def servicesJSON = file('google-services.json')
    if (servicesJSON.text) {
        apply plugin: 'com.google.gms.google-services'
    }
} catch(Exception e) {
    logger.info("google-services.json not found, google-services plugin not applied. Push Notifications won't work")
}
