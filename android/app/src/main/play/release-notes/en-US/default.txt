v1.4.6 — Duration UX polish

• Duration picker: Pixel-style "00h 00m 00s" layout — no colons, unit letters attached to each field. Minutes now focused by default (was seconds).
• Segment durations render as "01h 05m 30s" / "05m 03s" / "10s" — zero parts dropped, right-aligned so unit letters line up across every row.
• Editor TOTAL always shows HH:MM:SS for at-a-glance comparison.
• Running-timer clocks + chain-card totals unchanged.
