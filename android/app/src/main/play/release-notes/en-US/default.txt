v1.3.3 — chain rings through Do Not Disturb like an alarm

• Boundary chimes and the chain-end finale now route through the alarm audio stream, so chains keep ringing on silent or in Do Not Disturb — no per-app override needed.
• Notifications tagged as alarms so heads-up + lock-screen entries still appear under default DND policy.
• Fixed: notification-health probe was reporting "OK" while checking a deleted channel.
