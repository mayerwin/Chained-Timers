v1.4.10 — Edge swipe fix

• Swiping from either screen edge now returns you to the library instead of minimising the app. The system's gesture-nav back swipe bypasses the old Android back callback on Android 13+, so we now register the modern predictive-back callback too. Works from the run view and the editor.
