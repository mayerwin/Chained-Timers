v1.4.8 — Offline load & fixes

• Fonts bundled locally — app opens instantly even offline (previously white screen until Google Fonts timed out).
• Empty-name segments truly silent (native TTS was still saying "Segment").
• Swipe LEFT on run view now returns to library (was right only). Fixes the "swipe minimises the app" issue.
• Returning to a paused chain now shows the correct remaining time.
• Segment duration button reads "1m" (was "01m 00s").
