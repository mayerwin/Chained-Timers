v1.4.4 — Reliability + navigation

• Voice cues now honestly share the alarm-volume slider with the beeps (they used to sit on media volume, so voice sounded "always max"). Same output device foreground and background.
• Back arrow + swipe-left in the run view — return to the library without stopping the chain.
• Library now shows a live "now playing" strip for each running chain.
• Up to 5 concurrent chains (was 2).
• In-app update prompt via Play In-App Updates.
