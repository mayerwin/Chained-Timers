v1.3.8 — every chain cue honors the headset-routing setting

• Chime, 3-2-1 tick, chain-end finale, AND voice now ALL play through the output you picked in "When a headset is connected". Previous version only routed voice.
• Achieved by switching the cue pool from SoundPool to MediaPlayer instances kept prepared between plays — same low play latency, plus per-instance preferred-device binding.
• Flip the routing pill mid-chain — the cue pool re-binds on the next segment.
