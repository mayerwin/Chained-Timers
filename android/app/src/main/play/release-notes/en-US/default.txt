v1.4.9 — Swipe-back, inline rename, bulk delete

• Swipe-back on the run view now actually fires in either direction. Previous fix was correct in code but the WebView never dispatched the touch — fixed with touch-action: pan-y + explicit touch handlers.
• Tap the chain name on the live countdown to rename it inline (works even while running).
• Multi-select toolbar now has a trash icon left of Play for bulk delete, with a confirm dialog.
