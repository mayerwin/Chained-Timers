v1.4.2 — Cold-start voice + multi-select polish

• First segment now speaks its name on the very first run after a cold app start (TTS prerender race fix).
• Multi-select: stronger amber border on selected chains + Top/Below arrows showing which one will be focused vs run in the background.
• Fixes mojibake in editor segment numbers (№ glyph was rendering as garbled "â„–" after a file-encoding regression in v1.4.0).
