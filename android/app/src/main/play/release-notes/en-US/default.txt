v1.4.3 — Final mojibake cleanup

• Cleans up 15 remaining mojibake artifacts in source comments that the v1.4.2 fix missed (right-arrow →, warning sign ⚠, left-right arrow ↔). All in JavaScript comments — no runtime impact, but the source now matches what the published code does.
• Adds three scanner tools (tools/mojibake-{scan,diff,final}.mjs) so any future PowerShell-style encoding regression is caught immediately.
