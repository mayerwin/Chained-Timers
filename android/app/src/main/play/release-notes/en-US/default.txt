v1.4.17 — Long timers fit the screen

• Fixed the countdown running off the edge of the screen once a chain passed an hour. It now scales to fit whatever it has to show, stays centred, and makes better use of the width — so shorter times read larger too.
