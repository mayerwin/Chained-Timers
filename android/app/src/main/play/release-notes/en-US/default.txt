v1.4.1 — Per-chain background notifications

• Each running chain now has its own lock-screen notification — both stay live, with pause/skip/stop targeting the right chain.
• Stacked tray view with a "2 chains running" summary header.
• Per-chain MediaPlayer pool so audio for one can't interfere with the other.
• Fixes a TTS prerender collision when two chains share a segment name.

Builds on v1.4.0: long-press to select, chip strip, editor lock while running.
